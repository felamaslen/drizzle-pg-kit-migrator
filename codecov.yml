coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
    patch:
      default:
        target: 80%

ignore:
  - "dist"
  - "examples"
  - "tests"
  - "src/cli.ts"
  - "src/logger.ts"

comment:
  layout: "header, diff, files"
  require_changes: true
